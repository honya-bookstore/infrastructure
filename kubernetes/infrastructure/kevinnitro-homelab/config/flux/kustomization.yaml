---
# yaml-language-server: $schema=https://www.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: honyabookstore-
namespace: flux-system
resources:
  - ./webhook/
configurations:
  - ./kustomizeconfig.yaml
